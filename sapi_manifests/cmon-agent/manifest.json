{
	"name": "metric-agent",
	"path": "/opt/smartdc/agents/etc/metric-agent.config.json",
	"post_cmd": "[[ $(svcs -Ho state metric-agent) == 'online' ]] && /usr/sbin/svcadm restart metric-agent || /usr/sbin/svcadm enable metric-agent;"
}
